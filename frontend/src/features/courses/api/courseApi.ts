import { Course } from '@/features/courses/types/course.types';

export const courseApi = {
  async listCourses(): Promise<Course[]> {
    throw new Error('API học phần là skeleton, chưa kết nối backend.');
  },
};
