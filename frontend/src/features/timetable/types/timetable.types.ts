export interface TimetableSlot {
  day: number;
  period: number;
  courseCode: string;
  courseName: string;
  room: string;
  lecturer: string;
  classGroup: string;
  colorBg: string;
  colorBorder: string;
  colorText: string;
}
