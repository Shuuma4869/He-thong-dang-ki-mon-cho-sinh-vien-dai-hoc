import { NavigationTab } from '@/shared/types/navigation.types';

export const APP_ROUTES: Record<NavigationTab, string> = {
  dashboard: '/dashboard',
  courses: '/courses',
  registered: '/registered-courses',
  timetable: '/timetable',
  notifications: '/notifications',
  profile: '/profile',
};
