export const API_BASE_PATH = '/api';

export const API_ENDPOINTS = {
  auth: '/auth',
  courses: '/courses',
  registrations: '/registrations',
  profile: '/profile',
  timetable: '/timetable',
} as const;
