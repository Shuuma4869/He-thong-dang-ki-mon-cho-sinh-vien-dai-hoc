# Yêu cầu môi trường

Cần cài:

- Git.
- JDK 21.
- Node.js 20 trở lên.
- npm 10 trở lên.

Không cần cài Maven riêng vì backend dùng Maven Wrapper trong `backend/mvnw.cmd`.

Kiểm tra:

```powershell
git --version
java -version
javac -version
node -v
npm -v
```

